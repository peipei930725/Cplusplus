#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)

int main() {
	long long int num = 0;
	while (scanf("%d", &num) != EOF) {
		while (num > 10000000) {
			printf("%d")
		}
	}
	return 0;
}